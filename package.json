{
  "name": "crux",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@11.23.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "turbo run dev",
    "dev:server": "turbo run dev --filter=@crux/server",
    "dev:cockpit": "turbo run dev --filter=@crux/cockpit",
    "dev:site": "turbo run dev --filter=@crux/site",
    "build": "turbo run build",
    "lint": "biome check .",
    "format": "biome format --write .",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "db:migrate": "pnpm --filter @crux/server db:migrate",
    "db:seed": "pnpm --filter @crux/server db:seed"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "turbo": "^2.4.4"
  }
}
